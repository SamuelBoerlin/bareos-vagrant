#!/bin/bash
set -e
mkdir -p /tmp/build-tmp
cd /tmp/build-tmp
cmake -DGIT_DESCRIBE_VERSION="1.2.3" -DVERSION_STRING="1.2.3" -Dpostgresql=yes -Dsystemtest_db_user=$USER -Dtraymonitor=yes /mount/bareos/
ctest --verbose --rerun-failure --output-on-failure $@