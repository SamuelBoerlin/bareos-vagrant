#!/bin/bash
set -e
mkdir -p /tmp/build-tmp
cd /tmp/build-tmp
cmake -DGIT_DESCRIBE_VERSION="22.0.0" -DVERSION_STRING="22.0.0" -Dpostgresql=yes -Dsystemtest_db_user=$USER -Dtraymonitor=yes /mount/bareos/
sudo REGRESS_DEBUG=1 ctest --verbose --output-on-failure $@